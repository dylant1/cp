#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);


    int n;
    int a, b, c;
    int res = 0;

    cin >> n;

    while (--n) {
        cin >> a >> b >> c;
    }
    cout << res;
    return 0;
}
